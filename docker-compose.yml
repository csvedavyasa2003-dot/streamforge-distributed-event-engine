version: "3.9"

services:

  backend:
    build: ./backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"

  kafka:
    image: bitnami/kafka:latest

  prometheus:
    image: prom/prometheus

  grafana:
    image: grafana/grafana